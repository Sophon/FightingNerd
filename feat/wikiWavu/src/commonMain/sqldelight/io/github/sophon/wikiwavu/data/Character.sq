CREATE TABLE CharacterEntity (
    id TEXT NOT NULL PRIMARY KEY,
    gameId TEXT NOT NULL,
    remoteQueryId TEXT NOT NULL,
    wikiUrl TEXT NOT NULL,
    displayName TEXT NOT NULL,
    aliases TEXT NOT NULL,
    hp TEXT,
    umo TEXT NOT NULL,
    imagesIconUrl TEXT,
    imagesBannerUrl TEXT,
    consecutiveFailureCount INTEGER NOT NULL DEFAULT 0
);

insertCharacter:
INSERT OR REPLACE INTO CharacterEntity(id, gameId, remoteQueryId, wikiUrl, displayName, aliases, hp, umo, imagesIconUrl, imagesBannerUrl)
VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?);

selectAll:
SELECT *
FROM CharacterEntity;

deleteAll:
DELETE FROM CharacterEntity;

deleteById:
DELETE FROM CharacterEntity
WHERE id = ?;

incrementFailureCount:
UPDATE CharacterEntity
SET consecutiveFailureCount = consecutiveFailureCount + 1
WHERE id = ?;

resetFailureCount:
UPDATE CharacterEntity
SET consecutiveFailureCount = 0
WHERE id = ?;
